/*Need to install Promoted build plugin */

stage("Validate before Apply") {
                timeout(time:30, unit:'MINUTES') {
                    input 'Are you sure to promote this build to QA?'
                }
            }
