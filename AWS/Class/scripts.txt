#!/bin/bash
sudo su
yum update -y
yum install httpd -y
service httpd start
chkconfig httpd on
cd /var/www/html
echo "<html><h1>Hello! Welcome to AWS. This instance is from $(hostname -f)</h1></html>"  >  index.html


sudo apt update
sudo apt install apache2
sudo ufw app list
sudo ufw allow 'Apache'
sudo ufw status


#!/bin/bash
sudo apt update
sudo apt install apache2 -y
sudo su
cd /var/www/html
echo "<html><h1>Hello! Welcome to AWS. This instance is from $(hostname -f)</h1></html>"  >  index.html

For increasing the CPU Utilization:

openssl speed -multi $(grep -ci processor /proc/cpuinfo)



NetworkID, HostID
	
Wordpress script: 

#!/bin/bash
yum update -y
yum install httpd php php-mysql -y
amazon-linux-extras install php7.4
cd /var/www/html  
wget https://wordpress.org/wordpress-5.1.1.tar.gz
tar -xzf wordpress-5.1.1.tar.gz
cp -r wordpress/* /var/www/html/
rm -rf wordpress
rm -rf wordpress-5.1.1.tar.gz
chmod -R 755 wp-content
chown -R apache:apache wp-content
service httpd start
chkconfig httpd on












cd /var/www/html wp-config.php
#wget https://s3.amazon.com/bucketforwordpresslab/htaccess.txt
#mv htaccess.txt .htaccess




$r = Get-AzureRMResource -ResourceName lab-vm -ResourceGroupName 63-e60c46eb-applying-tags-to-vms-in-azure-using-po
Set-AzureRmResource -Tag @{ Dept="IT"; Environment="Test" } -ResourceId $r.ResourceId -Force
